# Deployment Artifacts

`scripts/deploy-studionet.ts` writes `studionet.json` here after a real finalized deployment succeeds.

Do not edit deployment artifacts by hand. A valid artifact must contain a real contract address, deployment transaction hash, RPC, chain ID, and success execution result.
